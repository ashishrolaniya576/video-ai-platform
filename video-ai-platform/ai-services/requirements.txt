# ── Core framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── Deep learning ─────────────────────────────────────────────────────────────
# Install PyTorch with CUDA support separately from https://pytorch.org/get-started/locally/
# For CPU only: pip install torch torchvision
# For CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0
PyYAML>=6.0

# ── Computer vision ───────────────────────────────────────────────────────────
# Note: ultralytics pulls opencv-python; opencv-python-headless is kept for
# server environments without a display. Both share the same cv2 module.
opencv-python-headless>=4.9.0
Pillow>=10.3.0
numpy>=1.26.4

# ── Scientific computing ──────────────────────────────────────────────────────
# scipy 1.14+ supports numpy 2.x without warnings
scipy>=1.13.0

# ── Lightning (PromptIR) ──────────────────────────────────────────────────────
pytorch-lightning==2.2.4
einops==0.7.0

# ── HeavyRainRemoval dependencies ────────────────────────────────────────────
tqdm==4.66.4
scikit-image==0.23.2
guided-filter-pytorch==3.7.5
tensorboard_logger==0.1.0
gdown==5.2.0

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil==5.9.8         # Optional: memory usage logging
httpx==0.27.0         # For async AI sub-service calls
yt-dlp>=2024.03.10    # For downloading YouTube/Vimeo URLs in backend
huggingface_hub>=0.20.0 # For auto-downloading AI models

# ── WebRTC ────────────────────────────────────────────────────────────────────
aiortc>=1.8.0
av>=12.0.0
